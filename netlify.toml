# Netlify configuration for ImmigrationClock (Next.js 14 App Router).
# Netlify auto-detects Next.js and applies its Next.js Runtime
# (@netlify/plugin-nextjs) — no need to declare the plugin explicitly.

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
