# ImmigrationClock data pipeline dependencies
pandas>=2.1
SQLAlchemy>=2.0
psycopg2-binary>=2.9
requests>=2.31
python-dotenv>=1.0

# Full-coverage WARN ingestion: 40+ state parsers (HTML/Excel/PDF). Optional —
# ingest_warn_layoffs.py shells out to its CLI and falls back to a sample if it
# is not installed. https://github.com/biglocalnews/warn-scraper
warn-scraper>=1.2
