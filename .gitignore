# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# vercel
.vercel/

# stale netlify CLI state (migrated away from Netlify)
.netlify/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# secrets — never commit credentials. Use .env (ignored above) or the host's
# environment-variable settings. `.env.example` is the ONLY file that may
# describe these keys, and only with placeholder values.
pass.txt
passwords.txt
secrets.txt
credentials.txt
*.key
*credentials*.json
service-account*.json
.env.backup

# prisma
/prisma/dev.db
/prisma/*.db-journal

# python
__pycache__/
*.py[cod]
.venv/
venv/
data_pipeline/.cache/
data_pipeline/downloads/

# typescript
*.tsbuildinfo

# editor
.vscode/
.idea/
/dev-server.log

# local Claude Code settings (personal, not shared)
.claude/settings.local.json
